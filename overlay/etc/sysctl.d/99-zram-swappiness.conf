# dietpi-set_swapfile always writes vm.swappiness=50 for zram (98-*.conf).
# DietPi's own default for disk swap is 1 ("prevent OOM only"); keep that
# on this 512 MiB board so swap is a last resort. Lexical order: 99 > 98.
vm.swappiness=1
